---
phase: 115-tracking-corrections-and-api-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - mesher/storage/queries.mpl
autonomous: true
requirements: [UPS-01, UPS-02, UPS-03]

must_haves:
  truths:
    - "ROADMAP Phase 109 success criteria reflect the positional API style actually implemented"
    - "get_project_id_by_key is absent from mesher/storage/queries.mpl"
    - "get_user_orgs is absent from mesher/storage/queries.mpl"
    - "No other .mpl file imports get_project_id_by_key or get_user_orgs"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Canonical phase success criteria"
      contains: "Repo.insert_or_update"
    - path: "mesher/storage/queries.mpl"
      provides: "All Mesher data queries"
      contains: "get_project_by_api_key"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "Phase 109 UPS-01..03 SC"
      via: "success criteria update"
      pattern: "insert_or_update.*positional"
    - from: "mesher/storage/queries.mpl"
      to: "removed dead functions"
      via: "deletion"
      pattern: "get_project_by_api_key.*pub fn"
---

<objective>
Accept the Phase 109 positional API style as canonical by updating ROADMAP.md success criteria, and remove two dead-code query functions from mesher/storage/queries.mpl.

Purpose: Phase 109 implemented positional APIs (Repo.insert_or_update, Repo.delete_where_returning, Query.where_sub) that differ in style from the keyword-option API specified in the original ROADMAP. The functionality is correct and verified. ROADMAP needs to reflect reality. Dead code functions were never imported anywhere.
Output: ROADMAP with corrected Phase 109 SC descriptions; queries.mpl with dead code removed.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v11.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Phase 109 ROADMAP success criteria — current (keyword-option style, never implemented): -->

Current SC1: "A Mesh program can call `Repo.insert(on_conflict: :update)` keyword-option API..."
Current SC2: "A Mesh program can call existing insert/update/delete with `returning: true` option..."
Current SC3: "A Mesh program can use `Query.where(sub: ...)` keyword-option to nest a subquery..."

<!-- Actual implemented API (positional style, verified working): -->

Implemented SC1: Repo.insert_or_update(pool, table, fields, conflict_targets, update_fields)
  - Generates: INSERT ... ON CONFLICT (...) DO UPDATE SET ... RETURNING *
  - Pipeline: registered in MIR, codegen, JIT, typechecker

Implemented SC2: Repo.delete_where_returning(pool, table, query)
  - Generates: DELETE FROM table WHERE ... RETURNING *
  - Returns deleted rows; insert/update RETURNING not needed (no mesher use case)

Implemented SC3: Query.where_sub(q, field, sub_query)
  - Generates: WHERE field IN (SELECT ...)
  - Correct parameter binding across outer + inner queries

<!-- Dead code to remove from mesher/storage/queries.mpl: -->

Function 1 (lines ~128-144): get_project_id_by_key
  - Never imported by any .mpl file in mesher/
  - Superceded by get_project_by_api_key (lines ~111-126) which returns full Project struct
  - Safe to remove: grep confirms zero import sites

Function 2 (lines ~266-274): get_user_orgs
  - Never imported by any .mpl file in mesher/
  - Simple ORM query that was never wired into any service
  - Safe to remove: grep confirms zero import sites
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP Phase 109 success criteria to reflect positional API</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read .planning/ROADMAP.md in full. Find the Phase 109 section ("### Phase 109: Upserts, RETURNING, and Subqueries"). Update the three success criteria to describe the actually-implemented positional API style:

Replace SC1 (currently describing `Repo.insert(on_conflict: :update)` keyword-option):
```
  1. A Mesh program can call `Repo.insert_or_update(pool, table, fields, conflict_targets, update_fields)` and it inserts a new row or updates the existing one, generating `INSERT ... ON CONFLICT (...) DO UPDATE SET ... RETURNING *`
```

Replace SC2 (currently describing `returning: true` option on existing functions):
```
  2. A Mesh program can call `Repo.delete_where_returning(pool, table, query)` to delete matching rows and receive the deleted rows back via `RETURNING *`
```

Replace SC3 (currently describing `Query.where(sub: ...)` keyword-option):
```
  3. A Mesh program can use `Query.where_sub(q, :field, sub_query)` to nest a subquery in a WHERE IN clause with correct parameter binding
```

These are purely descriptive corrections — the implementation is complete and verified. The acceptance note (below SC3 if present) should be updated or added to clarify these represent the canonical API shape for v11.0.

Also update the Phase 115 entry in the Phase Details section to show the correct phase goal and plans now that planning is complete:
- Plans: 2 plans
- Add plan list:
  - [ ] 115-01-PLAN.md — Requirements tracking corrections (WHERE/FRAG/UPS documentation)
  - [ ] 115-02-PLAN.md — ROADMAP API acceptance + dead code removal

Write the complete updated file using the Write tool.
  </action>
  <verify>
    <automated>grep -c "insert_or_update\|delete_where_returning\|where_sub" /Users/sn0w/Documents/dev/snow/.planning/ROADMAP.md</automated>
    <manual>Phase 109 success criteria should mention Repo.insert_or_update, Repo.delete_where_returning, and Query.where_sub (positional style). No keyword-option syntax (on_conflict:, returning:, sub:) should appear in Phase 109 SCs.</manual>
  </verify>
  <done>ROADMAP.md Phase 109 success criteria use positional API names (Repo.insert_or_update, Repo.delete_where_returning, Query.where_sub). Phase 115 plan list shows both plans.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code functions from queries.mpl</name>
  <files>mesher/storage/queries.mpl</files>
  <action>
First verify no import sites exist:
```bash
grep -r "get_project_id_by_key\|get_user_orgs" /Users/sn0w/Documents/dev/snow/mesher/ --include="*.mpl"
```
Expected: only the function definitions in queries.mpl (lines ~132 and ~267). If any other .mpl files import these, STOP and report — do not remove.

Assuming zero import sites confirmed, read mesher/storage/queries.mpl in full, then:

1. Remove the `get_project_id_by_key` function block (the entire function including its 3-line comment header):
   - Comment: "# Get the project ID associated with a valid (non-revoked) API key." (3 lines)
   - Function: `pub fn get_project_id_by_key(pool :: PoolHandle, key_value :: String) -> String!String do ... end`
   - The adjacent blank line separating it from the next function

2. Remove the `get_user_orgs` function block (the entire function including its 1-line comment header):
   - Comment: "# Get all organizations a user belongs to."
   - Function: `pub fn get_user_orgs(pool :: PoolHandle, user_id :: String) -> List<OrgMembership>!String do ... end`
   - The adjacent blank line separating it from the next function

The functions are:
- get_project_id_by_key: defined at line ~132, ~13 lines long
- get_user_orgs: defined at line ~267 (after removal of first function, line numbers shift), ~8 lines long

After removal, the file should still compile correctly (both functions had zero callers). Write the updated file using the Write tool.
  </action>
  <verify>
    <automated>grep -c "get_project_id_by_key\|get_user_orgs" /Users/sn0w/Documents/dev/snow/mesher/storage/queries.mpl</automated>
    <manual>Expected count: 0. Both function definitions removed. Run: cd /Users/sn0w/Documents/dev/snow && meshc build mesher to confirm zero compilation errors after removal.</manual>
  </verify>
  <done>get_project_id_by_key and get_user_orgs are absent from mesher/storage/queries.mpl. meshc build mesher compiles with zero errors.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

1. Verify ROADMAP uses positional API names in Phase 109:
   `grep "insert_or_update\|delete_where_returning\|where_sub" .planning/ROADMAP.md`
   Expected: 3+ matches in the Phase 109 success criteria section

2. Verify dead code removed from queries.mpl:
   `grep "get_project_id_by_key\|get_user_orgs" mesher/storage/queries.mpl`
   Expected: zero matches

3. Verify no broken imports across mesher:
   `grep -r "get_project_id_by_key\|get_user_orgs" mesher/ --include="*.mpl"`
   Expected: zero matches

4. Verify mesher still compiles:
   `cd /Users/sn0w/Documents/dev/snow && meshc build mesher 2>&1 | tail -5`
   Expected: "Compilation successful" or zero error lines
</verification>

<success_criteria>
- ROADMAP.md Phase 109 SC1 names Repo.insert_or_update (positional)
- ROADMAP.md Phase 109 SC2 names Repo.delete_where_returning (positional)
- ROADMAP.md Phase 109 SC3 names Query.where_sub (positional)
- mesher/storage/queries.mpl has zero occurrences of get_project_id_by_key or get_user_orgs
- meshc build mesher compiles with zero errors after removal
- Phase 115 plan list in ROADMAP shows both 115-01 and 115-02
</success_criteria>

<output>
After completion, create `.planning/phases/115-tracking-corrections-and-api-acceptance/115-02-SUMMARY.md`
</output>
