---
phase: 134-add-phase-132-s-change-to-the-documentation-site-and-update-the-appropriate-skill-s-in-tools-skill-mesh-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill/mesh/skills/strings/SKILL.md
  - tools/skill/mesh/SKILL.md
autonomous: true
requirements:
  - DOC-134-01

must_haves:
  truths:
    - "The strings skill has a ## JSON Literals section documenting json { } syntax, type table, nesting, auto-coercion, HTTP example, and reserved-keyword note"
    - "The strings skill frontmatter description mentions json literals"
    - "The strings skill ## Heredoc Strings section has a note pointing to json { } for JSON objects"
    - "The top-level mesh SKILL.md Language at a Glance item 4 lists json { } alongside heredocs"
    - "The top-level mesh SKILL.md Stdlib overview mentions the Json module and json { } literals"
    - "The top-level mesh SKILL.md Available Sub-Skills entry for strings mentions json literals"
  artifacts:
    - path: "tools/skill/mesh/skills/strings/SKILL.md"
      provides: "Complete json { } documentation for the strings sub-skill"
      contains: "## JSON Literals"
    - path: "tools/skill/mesh/SKILL.md"
      provides: "Top-level skill awareness of json { } across Language at a Glance, Stdlib, and routing"
      contains: "json { }"
  key_links:
    - from: "tools/skill/mesh/SKILL.md"
      to: "tools/skill/mesh/skills/strings/SKILL.md"
      via: "Available Sub-Skills routing description for strings"
      pattern: "strings.*json"
---

<objective>
Update the mesh strings sub-skill and top-level mesh skill to document the json { } native object literal feature added in phase 132.

Purpose: AI assistants using these skills need to know json { } exists and how to use it. Without this, they will continue to suggest heredoc/string concatenation patterns for JSON construction in Mesh code.
Output: Updated SKILL.md files with comprehensive json { } coverage in both the strings sub-skill and the top-level routing/overview skill.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tools/skill/mesh/skills/strings/SKILL.md
@tools/skill/mesh/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSON Literals section to strings SKILL.md</name>
  <files>tools/skill/mesh/skills/strings/SKILL.md</files>
  <action>
Make the following changes to tools/skill/mesh/skills/strings/SKILL.md:

1. Update the frontmatter `description` field to mention json literals:
   Change: `description: Mesh string features: interpolation (#{} and ${}), heredocs, String stdlib, Env.get/get_int, and the Regex module.`
   To:     `description: Mesh string features: interpolation (#{} and ${}), heredocs, json { } object literals, String stdlib, Env.get/get_int, and the Regex module.`

2. In the `## Heredoc Strings` section, after the existing code example block (after the line `# Outputs: {"id": 42, "name": "Alice"}`), add a note before the next section:

```
> **For JSON objects:** Prefer `json { }` literals over heredoc JSON templates — they are type-safe and require no manual escaping. See **JSON Literals** below.
```

3. Add a new `## JSON Literals` section between the `## Heredoc Strings` section and the `## String Stdlib` section. The full content to insert:

```markdown
## JSON Literals

Rules:
1. `json { key: value, ... }` — constructs a JSON object. Keys are bare identifiers; values are any Mesh expression.
2. Multi-line syntax works identically — newlines inside `{ }` are insignificant.
3. The result is a `Json` type that auto-coerces to `String` — pass directly to `HTTP.response`, `Ws.broadcast`, or any function expecting a String.
4. Nesting: assign `json { }` to a variable then use it as a field value — embedded raw, no double-encoding.
5. Reserved keywords (`type`, `fn`, `let`, etc.) cannot be used as bare keys — use heredoc strings for those cases.

Type serialization table:

| Mesh type | JSON output |
|-----------|-------------|
| `String`  | `"quoted string"` |
| `Int`     | `42` (unquoted number) |
| `Float`   | `3.14` (unquoted) |
| `Bool`    | `true` / `false` |
| `nil`     | `null` |
| `Option<T>` | `null` (None) or the serialized value (Some) |
| `List<T>` | JSON array |
| Struct with `deriving(Json)` | nested JSON object |

Code example (from tests/e2e/json_literal_basic.mpl and phase 132 Mesher migration):
```mesh
# Basic object — auto-coerces to String
HTTP.response(200, json { status: "ok", count: 42 })
HTTP.response(401, json { error: "unauthorized" })

# Multi-line
let event = json {
  issue_id: issue_id,
  severity: "high",
  active: true
}

# Nesting — inner embedded raw, no double-encoding
let inner = json { code: 200 }
let outer = json { result: inner, ok: true }
# outer is: {"result":{"code":200},"ok":true}

# Option field
let data = json { value: some_option }  # None -> null, Some(v) -> v

# List<String> as array
let tags = ["a", "b", "c"]
let obj = json { tags: tags }  # {"tags":["a","b","c"]}
```
```

(Note: the triple-backtick code fence inside the section should be a normal Mesh code block — no backtick escaping issues since it is plain Markdown.)

The section must be inserted AFTER the closing ``` of the Heredoc Strings code example and the note you added, and BEFORE the `## String Stdlib` heading.
  </action>
  <verify>
    <automated>grep -c "## JSON Literals" /Users/sn0w/Documents/dev/snow/tools/skill/mesh/skills/strings/SKILL.md && grep -c "json literals" /Users/sn0w/Documents/dev/snow/tools/skill/mesh/skills/strings/SKILL.md && grep -c "Type serialization table" /Users/sn0w/Documents/dev/snow/tools/skill/mesh/skills/strings/SKILL.md</automated>
  </verify>
  <done>
    - frontmatter description includes "json { } object literals"
    - ## JSON Literals section exists with rules, type table, and code example
    - ## Heredoc Strings section has a note pointing to json { }
    - Section appears between ## Heredoc Strings and ## String Stdlib
  </done>
</task>

<task type="auto">
  <name>Task 2: Update top-level mesh SKILL.md for json { } awareness</name>
  <files>tools/skill/mesh/SKILL.md</files>
  <action>
Make the following targeted changes to tools/skill/mesh/SKILL.md:

1. **Language at a Glance — item 4:** Update to list json { } alongside heredocs.
   Current item 4: `4. Heredocs: `"""..."""` for multiline strings with embedded interpolation.`
   New item 4:     `4. Heredocs: `"""..."""` for multiline strings with embedded interpolation; `json { }` for JSON object literals (type-safe, auto-coerces to String).`

2. **Ecosystem Overview — item 4 (Stdlib):** Add Json module and json { } mention.
   Current item 4: `4. Stdlib: List, Map, Set, Range, Queue, Iter (pipeline), String, Json, IO, Env, Regex modules.`
   New item 4:     `4. Stdlib: List, Map, Set, Range, Queue, Iter (pipeline), String, Json (encode/parse + `json { }` literals), IO, Env, Regex modules.`

3. **Available Sub-Skills — item 9 (strings):** Add json literals to the description.
   Current item 9: `9. `skills/strings` — String interpolation, heredocs, String stdlib, Env vars, Regex`
   New item 9:     `9. `skills/strings` — String interpolation, heredocs, `json { }` object literals, String stdlib, Env vars, Regex`
  </action>
  <verify>
    <automated>grep -c "json { }" /Users/sn0w/Documents/dev/snow/tools/skill/mesh/SKILL.md</automated>
  </verify>
  <done>
    - Language at a Glance item 4 references json { } alongside heredocs
    - Stdlib overview item 4 mentions json { } literals and the Json module
    - Sub-Skills item 9 (strings) description includes json { } object literals
    - All three occurrences of json { } present in the file
  </done>
</task>

</tasks>

<verification>
Run after both tasks:
```bash
grep -n "## JSON Literals" /Users/sn0w/Documents/dev/snow/tools/skill/mesh/skills/strings/SKILL.md
grep -n "json { }" /Users/sn0w/Documents/dev/snow/tools/skill/mesh/SKILL.md
grep -n "json literals" /Users/sn0w/Documents/dev/snow/tools/skill/mesh/skills/strings/SKILL.md
```
Expected: at least 1 match for each command.
</verification>

<success_criteria>
- tools/skill/mesh/skills/strings/SKILL.md has ## JSON Literals section with rules, type table, code examples, and nesting demonstration
- tools/skill/mesh/skills/strings/SKILL.md frontmatter description mentions json literals
- tools/skill/mesh/skills/strings/SKILL.md ## Heredoc Strings section points to json { } for JSON objects
- tools/skill/mesh/SKILL.md Language at a Glance item 4 includes json { }
- tools/skill/mesh/SKILL.md Stdlib overview mentions json { } literals
- tools/skill/mesh/SKILL.md strings sub-skill routing description includes json literals
</success_criteria>

<output>
After completion, create `.planning/phases/134-add-phase-132-s-change-to-the-documentation-site-and-update-the-appropriate-skill-s-in-tools-skill-mesh-skills/134-01-SUMMARY.md`
</output>
